import 'package:flutter/material.dart';

class Theme {
  static ThemeData _kTheme = ThemeData(
    brightness: Brightness.light,
    primarySwatch: Colors.teal,
    accentColor: Colors.redAccent,
  );
}
